version: '3.8'

services:
<<<<<<< HEAD
=======
  redis:
    image: redis:7-alpine
    container_name: sosguinee_redis
    restart: always

>>>>>>> chore/security-design-hardening
  nginx:
    image: nginx:latest
    container_name: sosguinee_nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - static_volume:/app/staticfiles:ro
      - media_volume:/app/mediafiles
    depends_on:
      - web
    restart: always
    
  db:
    image: postgres:15
    container_name: sosguinee_db
    environment:
<<<<<<< HEAD
      - POSTGRES_DB=sosguinee
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=SEREma@2025
=======
      - POSTGRES_DB=${POSTGRES_DB:-sosguinee}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
>>>>>>> chore/security-design-hardening
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

  web:
    build: .
    container_name: sosguinee_web
<<<<<<< HEAD
=======
    env_file:
      - .env
>>>>>>> chore/security-design-hardening
    volumes:
      - .:/app
      - static_volume:/app/staticfiles
      - media_volume:/app/mediafiles
    ports:
      - "8000:8000"
    environment:
<<<<<<< HEAD
      - DEBUG=0
      - USE_POSTGRES=True
      - POSTGRES_NAME=sosguinee
      - POSTGRES_DB=sosguinee
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=SEREma@2025
      - POSTGRES_PORT=5432
      - ALLOWED_HOSTS=127.0.0.1,localhost,18.170.114.4
    depends_on:
      - db
    restart: always

=======
      - DJANGO_SETTINGS_MODULE=sosguinee.settings_prod
      - ENVIRONMENT=production
      - DEBUG=0
      - SECRET_KEY=${SECRET_KEY}
      - USE_POSTGRES=True
      - POSTGRES_NAME=${POSTGRES_NAME:-sosguinee}
      - POSTGRES_DB=${POSTGRES_DB:-sosguinee}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - ALLOWED_HOSTS=${ALLOWED_HOSTS:-127.0.0.1,localhost}
      - CSRF_TRUSTED_ORIGINS=${CSRF_TRUSTED_ORIGINS:-http://localhost:7400,http://127.0.0.1:7400}
      - CELERY_BROKER_URL=${CELERY_BROKER_URL:-redis://redis:6379/0}
      - CELERY_RESULT_BACKEND=${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      - ASYNC_EMAIL_ENABLED=${ASYNC_EMAIL_ENABLED:-True}
    depends_on:
      - db
      - redis
    restart: always

  celery_worker:
    build: .
    container_name: sosguinee_celery_worker
    command: celery -A sosguinee worker -l info -Q default,email
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - DJANGO_SETTINGS_MODULE=sosguinee.settings_prod
      - ENVIRONMENT=production
      - DEBUG=0
      - SECRET_KEY=${SECRET_KEY}
      - USE_POSTGRES=True
      - POSTGRES_NAME=${POSTGRES_NAME:-sosguinee}
      - POSTGRES_DB=${POSTGRES_DB:-sosguinee}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - CELERY_BROKER_URL=${CELERY_BROKER_URL:-redis://redis:6379/0}
      - CELERY_RESULT_BACKEND=${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      - ASYNC_EMAIL_ENABLED=${ASYNC_EMAIL_ENABLED:-True}
    depends_on:
      - db
      - redis
    restart: always

  celery_beat:
    build: .
    container_name: sosguinee_celery_beat
    command: celery -A sosguinee beat -l info
    env_file:
      - .env
    volumes:
      - .:/app
    environment:
      - DJANGO_SETTINGS_MODULE=sosguinee.settings_prod
      - ENVIRONMENT=production
      - DEBUG=0
      - SECRET_KEY=${SECRET_KEY}
      - USE_POSTGRES=True
      - POSTGRES_NAME=${POSTGRES_NAME:-sosguinee}
      - POSTGRES_DB=${POSTGRES_DB:-sosguinee}
      - POSTGRES_USER=${POSTGRES_USER:-postgres}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_PORT=${POSTGRES_PORT:-5432}
      - CELERY_BROKER_URL=${CELERY_BROKER_URL:-redis://redis:6379/0}
      - CELERY_RESULT_BACKEND=${CELERY_RESULT_BACKEND:-redis://redis:6379/0}
      - ASYNC_EMAIL_ENABLED=${ASYNC_EMAIL_ENABLED:-True}
    depends_on:
      - db
      - redis
    restart: always

>>>>>>> chore/security-design-hardening
volumes:
  pgdata:
  static_volume:
  media_volume:
