{% block extrajs %}
<script>
  document.addEventListener('DOMContentLoaded', function() {
    // Initialisation des selects Materialize
    var elemsSelect = document.querySelectorAll('select');
    M.FormSelect.init(elemsSelect);

    // Initialisation des datepickers Materialize avec localisation française
    var elemsDatepicker = document.querySelectorAll('.datepicker');
    M.Datepicker.init(elemsDatepicker, {
      format: 'yyyy-mm-dd', // Format de date pour Django
      autoClose: true,
      i18n: {
        cancel: 'Annuler',
        clear: 'Effacer',
        done: 'OK',
        months: [
          'Janvier', 'Février', 'Mars', 'Avril', 'Mai', 'Juin', 'Juillet', 'Août', 'Septembre', 'Octobre', 'Novembre', 'Décembre'
        ],
        monthsShort: [
          'Jan', 'Fév', 'Mar', 'Avr', 'Mai', 'Juin', 'Juil', 'Août', 'Sep', 'Oct', 'Nov', 'Déc'
        ],
        weekdays: [
          'Dimanche', 'Lundi', 'Mardi', 'Mercredi', 'Jeudi', 'Vendredi', 'Samedi'
        ],
        weekdaysShort: [
          'Dim', 'Lun', 'Mar', 'Mer', 'Jeu', 'Ven', 'Sam'
        ],
        weekdaysAbbrev: ['D', 'L', 'M', 'M', 'J', 'V', 'S']
      }
    });

    // Initialisation du Parallax
    var elemsParallax = document.querySelectorAll('.parallax');
    M.Parallax.init(elemsParallax);
  });
</script>
{# Assuming js/loss_alerts.js handles AJAX filtering if implemented #}
<script src="{% static 'js/loss_alerts.js' %}"></script>
{% endblock %}


<p><strong>Date :</strong> {{ item.date_alert|date:"d F Y" }}</p>


/* Hero Section for List Pages */
.list-hero-section {
    min-height: 400px; /* Height for list page hero */
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
}

.list-hero-content {
    z-index: 10;
    position: relative;
    padding: 25px 35px; /* Augmenter le padding pour un fond plus large */
    background: rgba(0, 0, 0, 0.5); /* Fond semi-transparent */
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3); /* Ombre pour la profondeur */
}

.list-hero-content h1, .list-hero-content h5 {
    margin-top: 0;
    margin-bottom: 15px;
}

.list-hero-content .btn-large {
    margin: 10px;
    font-weight: bold;
    text-transform: uppercase;
}

/* Assurez-vous que le texte a une couleur claire pour contraster avec le fond sombre */
.list-hero-content h1,
.list-hero-content h5 {
    color: white; /* Texte blanc */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7); /* Ombre pour le texte */
}


/* Main Image Card on Detail Page */
.detail-image-card .card-image {
    height: 400px; /* Hauteur fixe pour l'image principale */
    overflow: hidden;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #f0f0f0; /* Couleur de fond si object-fit: contain est utilisé */
}

.detail-image-card .card-image img {
    width: 100%;
    height: 100%;
    object-fit: contain; /* L'image est entièrement contenue, peut laisser des espaces */
    display: block;
}


/* Hero Section for Detail Page */
.detail-hero-section {
    min-height: 300px; /* Smaller height for detail page hero */
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
}

.detail-hero-content {
    z-index: 10;
    position: relative;
    padding: 25px 35px; /* Augmenter le padding */
    background: rgba(0, 0, 0, 0.5); /* Fond semi-transparent */
    border-radius: 8px;
    box-shadow: 0 4px 10px rgba(0, 0, 0, 0.3); /* Ombre pour la profondeur */
}

.detail-hero-content h1, .detail-hero-content h5 {
    margin-top: 0;
    margin-bottom: 15px;
    color: white; /* Texte blanc */
    text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7); /* Ombre pour le texte */
}